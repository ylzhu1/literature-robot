# The scheduled workflow (literature_agent.main) uses only the Python standard library.
# The setup desktop GUI uses CustomTkinter for its modern interface.
customtkinter>=6.0.0

# PyInstaller is required only when building the Windows release package.
# pyinstaller
